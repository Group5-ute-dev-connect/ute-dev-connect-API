info:
  name: Delete Banned Word
  type: http
  seq: 3

http:
  method: DELETE
  url: http://localhost:5000/api/filters/words/toxicword
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
