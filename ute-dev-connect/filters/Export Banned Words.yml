info:
  name: Export Banned Words
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:5000/api/filters/export
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
