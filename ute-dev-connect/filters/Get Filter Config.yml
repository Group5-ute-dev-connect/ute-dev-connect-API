info:
  name: Get Filter Config
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:5000/api/filters
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
