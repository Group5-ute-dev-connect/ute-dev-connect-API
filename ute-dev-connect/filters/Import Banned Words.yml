info:
  name: Import Banned Words
  type: http
  seq: 8

http:
  method: POST
  url: http://localhost:5000/api/filters/import
  headers: []
  body:
    type: multipartForm
    data:
      - name: file
        value: ""
        type: file
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
