info:
  name: Get Messages
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:5000/api/chat/:conversationId/messages
  body:
    type: none
  auth:
    type: bearer
    token: '{{token}}'

settings:
  encodeUrl: true
