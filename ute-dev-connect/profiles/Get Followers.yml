info:
  name: Get Followers
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:5000/api/profile/followers/651a1b2c3d4e5f60718293a4
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
