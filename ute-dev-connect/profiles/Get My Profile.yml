info:
  name: Get My Profile
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:5000/api/profile/me
  body:
    type: none
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
