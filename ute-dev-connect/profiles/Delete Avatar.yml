info:
  name: Delete Avatar
  type: http
  seq: 11

http:
  method: DELETE
  url: http://localhost:5000/api/profile/avatar
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
