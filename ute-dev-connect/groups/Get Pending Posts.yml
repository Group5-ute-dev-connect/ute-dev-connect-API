info:
  name: Get Pending Posts
  type: http
  seq: 15

http:
  method: GET
  url: http://localhost:5000/api/groups/6a12f298e69915b822f93509/pending-posts
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
