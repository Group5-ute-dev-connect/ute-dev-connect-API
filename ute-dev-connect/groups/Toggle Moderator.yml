info:
  name: Toggle Moderator
  type: http
  seq: 14

http:
  method: PUT
  url: http://localhost:5000/api/groups/6a12f298e69915b822f93509/moderator
  headers:
    - name: Content-Type
      value: application/json
  auth:
    type: bearer
    token: "{{token}}"
  body:
    type: json
    data: |-
      {
        "userId": "651a3a1b2c3d4e5f60718290"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
