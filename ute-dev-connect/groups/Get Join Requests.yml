info:
  name: Get Join Requests
  type: http
  seq: 10

http:
  method: GET
  url: http://localhost:5000/api/groups/6a12f298e69915b822f93509/join-requests
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
