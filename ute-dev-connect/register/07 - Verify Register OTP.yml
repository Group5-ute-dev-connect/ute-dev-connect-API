info:
  name: Verify Register OTP
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:5000/api/auth/verify-register-otp
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "teststudent@student.hcmute.edu.vn",
        "otp": "123456"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
