info:
  name: Get User Posts
  type: http
  seq: 21

http:
  method: GET
  url: http://localhost:5000/api/posts/user/651a3a1b2c3d4e5f60718290
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
