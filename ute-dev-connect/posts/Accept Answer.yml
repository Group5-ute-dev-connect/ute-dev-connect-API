info:
  name: Accept Answer
  type: http
  seq: 10

http:
  method: PUT
  url: http://localhost:5000/api/posts/accept/REPLACE_POST_ID/REPLACE_COMMENT_ID
  body:
    type: none
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
