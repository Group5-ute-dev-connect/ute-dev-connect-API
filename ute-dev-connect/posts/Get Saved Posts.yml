info:
  name: Get Saved Posts
  type: http
  seq: 11

http:
  method: GET
  url: http://localhost:5000/api/posts/saved
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
