info:
  name: Get System Logs
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:5000/api/admin/logs?page=1&limit=15
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
