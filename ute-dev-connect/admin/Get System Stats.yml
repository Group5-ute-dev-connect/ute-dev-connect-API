info:
  name: Get System Stats
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:5000/api/admin/stats?preset=7d
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
