info:
  name: Get Unread Count
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:5000/api/notifications/unread-count
  auth:
    type: bearer
    token: '{{token}}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
