info:
  name: Mark All Read
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:5000/api/notifications/read-all
  auth:
    type: bearer
    token: '{{token}}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
